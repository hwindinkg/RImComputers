namespace RimComputers
{
    public enum ComputerState
    {
        Off,
        Booting,
        Running,
        Error,
        NoPower
    }

    public enum LuaVersion
    {
        Lua52,
        Lua53
    }
}
